#pragma warning(disable:4996)
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    int a[2];1
    int c = 0;
    1
    scanf("%d %d", &a[0], &a[1]);


    if (a[0] > a[1])
        printf("%d%d", a[0], a[1]);
    else
        printf("%d%d", a[1], a[0]);
}                     